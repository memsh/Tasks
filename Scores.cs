using UnityEngine;
namespace Task29
{
    public class Scores : MonoBehaviour
    {
        void Start()
        {
            string myString = "Hello, World!";
            bool myBool = true;

            // Use the variables
            if (myBool)
            {
                Debug.Log(myString);
            }
        }
    }
}
