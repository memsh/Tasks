using UnityEngine;
namespace Task29
{
    public class Scores : MonoBehaviour
    {
        void Start()
        {
            string myString = "Gooool...!";
            bool myBool = true;

            // Use the variables
            if (myBool)
            {
                Debug.Log(myString);
            }
        }
    }
}
